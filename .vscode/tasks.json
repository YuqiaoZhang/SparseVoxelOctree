{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-glibc-debug-x64",
            "type": "shell",
            "command": "${workspaceFolder}/WIN/SVO/build.sh debug x64",
            "problemMatcher": [],
            "group": "build"
        }
    ]
}